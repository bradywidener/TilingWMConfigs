# ===========================
# Variables
# ===========================
# Main modifier
$mainMod = ALT

# ===========================
# Window focus (like Alt+H/J/K/L)
# ===========================
bind = $mainMod, H, movefocus, l
bind = $mainMod, J, movefocus, d
bind = $mainMod, K, movefocus, u
bind = $mainMod, L, movefocus, r

# Also map arrow keys for convenience
bind = $mainMod, LEFT, movefocus, l
bind = $mainMod, DOWN, movefocus, d
bind = $mainMod, UP, movefocus, u
bind = $mainMod, RIGHT, movefocus, r

# ===========================
# Move active window (like Alt+Shift+H/J/K/L)
# ===========================
bind = $mainMod SHIFT, H, movewindow, l
bind = $mainMod SHIFT, J, movewindow, d
bind = $mainMod SHIFT, K, movewindow, u
bind = $mainMod SHIFT, L, movewindow, r

# Arrow versions too
bind = $mainMod SHIFT, LEFT, movewindow, l
bind = $mainMod SHIFT, DOWN, movewindow, d
bind = $mainMod SHIFT, UP, movewindow, u
bind = $mainMod SHIFT, RIGHT, movewindow, r

#Rotation
bind = $mainMod SHIFT, J, layoutmsg, rotate -1
bind = $mainMod SHIFT, K, layoutmsg, rotate 1


# ===========================
# Workspace switching (like Alt+1..9)
# ===========================
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to workspace (like Alt+Shift+1..9)
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# ===========================
# Toggle floating (like Alt+Shift+F)
# ===========================
bind = $mainMod SHIFT, F, togglefloating

# ===========================
# Close window (like Alt+Shift+Q)
# ===========================
bind = CTRL, Q, killactive

# ===========================
# Shortcuts and AutoLaunch
# ===========================

# Shortcuts
bind = $mainMod CTRL, RETURN, exec, alacritty
bind = $mainMod CTRL, B, exec, firefox
bind = $mainMod CTRL, M, exec, flatpak run com.spotify.Client
bind = $mainMod CTRL, F, exec, thunar
bind = $mainMod CTRL, C, exec, code
bind = $mainMod CTRL, Y, exec, firefox https://chatgpt.com
bind = CTRL, SPACE, exec, hyprlauncher

# Reload Hyprland config
bind = $mainMod, R, exec, hyprctl reload
# Logout
bind = $mainMod CTRL, DELETE, exec, hyprctl dispatch exit

#Autolaunch
exec-once = ashell
exec-once = waypaper --restore
